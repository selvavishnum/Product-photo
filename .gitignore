# Editors / OS
*.iml
.idea/
.DS_Store

# Android build output. Still needed after the native Kotlin app was
# removed: mobile/ generates its own android/ folder in CI and Gradle runs
# underneath Flutter there.
.gradle/
local.properties
/captures
*.apk
*.aab

# Python backend
backend/__pycache__/
backend/**/__pycache__/
backend/.pytest_cache/
backend/venv/
backend/.venv/
*.pyc

# Claude Code harness state (e.g. isolated git worktrees for subagents)
.claude/

# Flutter (mobile/) -- android/ and ios/ are generated by CI, not committed
# (see mobile/README.md); these are just standard build-tool output.
mobile/.dart_tool/
mobile/build/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
